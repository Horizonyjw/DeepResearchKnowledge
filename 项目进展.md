# 项目进展记录（DeepResearchKnowledge）


## 2026/01/23 - 2026/02/03：项目基础搭建

- 新增前端：`frontend/src/pages/Home.tsx`、`LoginPage.tsx`、`SearchResultsPage.tsx`。
  - 功能：展示首页搜索框、热门标签、登录/注册入口、历史记录提示。
  - 主要接口：前端路由 `/` -> 首页；`/login` -> 登录/注册；`/search` -> 搜索结果页。
- 新增前端组件：`Navbar`, `SearchCard`, `UserCenter` 等。
- 新增后端基础 Express：`backend/src/index.js`、`backend/src/app.js`（API 基础中间件、CORS、日志等）。
- 新增后端路由文件：`backend/src/routes/auth-routes.js`、`paper-routes.js`、`report-routes.js`。
- 新增后端服务模板：`backend/src/services/auth-service.js`、`paper-service.js`、`report-service.js`（只搭建空白骨架，后续补充）。

## 2026/02/04 - 2026/02/11：前后端用户认证与搜索联通

- 完成前端用户认证流程：
  - `frontend/src/lib/api.ts` 包含 `login`, `register`, `logout`, `fetchSearchHistory` 等接口调用。
  - 本地存储 token、用户信息及偏好设置。
- 完成后端认证 CRUD：
  - `backend/src/routes/auth-routes.js`：`POST /api/auth/login`, `POST /api/auth/register`, `POST /api/auth/logout`, `GET /api/auth/me`。
  - `backend/src/middleware/auth.js`：JWT 解析与 `tryAuth` 保护接口。
  - `backend/src/services/history-service.js`：记录搜索/生成报告历史。
- 完成论文搜索接口 `GET /api/papers/search`：
  - 实现参数处理：`query`, `type`, `year`, `author`, `source`, `sortBy`, `page`, `pageSize`。
  - 结果结构：`results`, `total`, `page`, `pageSize`, `facets`。
  - 结果保存在历史 `addHistory`。

## 2026/02/23 - 2026/03/02：
### 搜索服务核心逻辑与爬取模块集成

- `backend/src/services/paper-service.js` 全面实现搜索逻辑：
  - 核心函数 `searchPapers(params)`。
  - 中文关键字、别名扩展 `expandQuery`、`tokenize`、`scoreOverlap`、`computeRelevanceScore`。
  - `OpenAlex` 查询：`fetchOpenAlexResults`。
  - `TopicCrawler`（论文爬取器）调用：`fetchTopicCrawlerResults`（基于 `backend/database/topic_crawler.py`）。
  - 结果去重 `dedupeByTitle`、过滤与排名 `matchesFilters` + `buildComparator` + `aggregateFacets`。
  - 本地种子数据兜底：`backend/src/data/papers.json`。
- 追加候选数据源：OpenAlex + arXiv（`topic_crawler.py`）
  - `TopicCrawler.get_query`：调用 GLM-4.7 转换用户中文查询为 arXiv 关键词/类别（如 cs.CL/cs.AI）。
  - `search_papers`：`paperscraper` 采集 arXiv，缓存 `cache.pkl`，自动补全 PDF/摘要等。
  - `rank_papers`：引用数获取（Semantic Scholar、CrossRef、OpenAlex）、时间加权。
- 每日最新文献抽取：`backend/database/daily_crawler.py`（调度、历史存储、筛选 topN，可结合定时任务）。

### AI 报告生成功能接入与增强

- `backend/src/services/report-service.js` 实现：
  - 核心函数 `buildReport(params)`。
  - 先尝试调用 Python 侧 `backend/API` 的 OpenAI/Dify 流式生成（`buildReportViaPython` + SSE）。
  - 失败或质量不达标时 `buildFallbackReport` 本地拼接：摘要、现状、代表研究、比较分析、挑战、展望、结论、参考文献。
- `backend/API` 模块：
  - `config.py`（API_KEY、URL、headers、payload template）。
  - `sse_client.py` / `workspace`：稳健流式返回文本。
  - `file_tool.py` / `upload_PDF.py` / `writer_tool.py`：PDF 抽取、文件处理、报告输出。
- `POST /api/reports/generate` 接口：
  - 请求体 `{ query, papers[], reportType }`。
  - 记录历史 `addHistory`、返回 `{ reportContent }`。

## 2026/03/03 - 2026/03/09：前端增强和智能搜索体验完善

- 搜索结果页：`SearchResultsPage.tsx` 大量优化展示结果卡片、分页、筛选、排序。
- 报告生成页：`ReportGenerationPage.tsx` 集成
  - 选择报告类型 `summary | detailed | comparative`。
  - 结果显示、流式加载动画、提示用户“正在生成”。
- `AuthContext` + `UserCenterPage` +偏好设置
  - `frontend/src/stores`/`contexts`：全局 auth 状态、用户信息、偏好接口（深度、主题、语言）。
  - `PreferenceSettingsPage.tsx` 支持用户定制 `pageSize`、`asyncFetch` 等。
- 后端增强：`backend/src/routes/users-routes.js`、`services/user-service.js`：用户信息读取/更新/密码修改。
- 后端测试补充：`backend/tests/postman`、`backend/tests/curl`、`backend/scripts/run-newman.js` 方便验证接口。

## 2026/03/10 - 2026/03/17：智能体核心能力与外部适配完善

- 智能体核心：`backend/database/topic_crawler.py` 和 `backend/src/services/paper-service.js` 组合形成“智能检索引擎”：
  - 中文语义查询 -> GLM 语义转换 -> arXiv/Topik爬取 -> OpenAlex补齐 -> 评分排序 -> 结果过滤。
  - 成果覆盖：关键字扩展、领域别名、近实时结果、罕见语种处理。
- 外围适配：后端 Express 主要作为“适配层”，与 Python 任务流程对接。
  - `backend/src/services/python-bridge.js`：`runPythonJson/runPythonText` 统一执行 Python 子进程，并以 `START_MARKER/END_MARKER` 解析内容。
  - `backend/config.js`：`databaseApiPath`, `reportApiPath`, 超时调整、请求限流。
- 日志与恢复：`backend/src/utils/http-error.js`、`backend/src/utils/validators.js`，统一错误码、明晰前端错误提示。
